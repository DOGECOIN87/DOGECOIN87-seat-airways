name = "seat-airlines-banners"
main = "src/index.ts"
compatibility_date = "2025-09-01"

# The published wall: one small JSON record per wallet.
[[kv_namespaces]]
binding = "BANNERS"
id = "0767939f209a41ab9692f2b1b1550c05"

# The artwork.
[[r2_buckets]]
binding = "IMAGES"
bucket_name = "seat-airlines-banners"

# The cabin directory: holder cards and the introductions between them.
#
#   npx wrangler d1 create seat-airlines-directory
#   npx wrangler d1 migrations apply seat-airlines-directory --remote
#
# Then uncomment this with the id the first command printed, and redeploy.
#
# It is commented out rather than left with a placeholder id on purpose: a
# binding naming a database that does not exist fails the deploy outright, and
# this file is what CI deploys on every push. Unbound, the wall works exactly
# as before and the directory routes answer 503 saying there is none — so the
# Worker is deployable before anyone has created the database.
#
# [[d1_databases]]
# binding = "DIRECTORY"
# database_name = "seat-airlines-directory"
# database_id = "…"

[vars]
# The Worker uses R2 only when this is set as well as the binding above, and
# falls back to serving images out of KV when it is not. That is deliberate:
# R2 is useful here precisely because images can be read straight from the
# bucket without touching the Worker, and that needs a public URL. A bound
# bucket with no public URL would mean paying R2's write path for none of its
# read benefit.
#
# To finish: R2 → seat-airlines-banners → Settings → Public access → enable
# r2.dev, then put that URL here and redeploy. Until then KV serves the
# images and everything works.
# PUBLIC_IMAGE_BASE = "https://pub-XXXXXXXX.r2.dev"

# The origins allowed to call this. Left empty the Worker echoes whatever
# origin asks, which is fine locally and careless in production.
ALLOWED_ORIGINS = "https://seat-airlines.space,https://www.seat-airlines.space"

# The token. Together with the RPC_URL secret below, lets the Worker refuse
# adverts from wallets that do not hold it; without that secret the check is
# skipped, and adverts from non-holders still never display on the page.
TOKEN_MINT = "AWJCyg9PrMtYju9yaQmdQLcwrGobHMTv9JU3mo4upump"
# RPC_URL is a secret rather than a var, since paid endpoints carry a key in
# the URL:  npx wrangler secret put RPC_URL
