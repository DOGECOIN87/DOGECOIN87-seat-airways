*.local
*.log
.DS_Store
.env
.env.*
.vite
dist
dist-ssr
node_modules

# After the .env.* above, not before it — git takes the last pattern that
# matches, so a negation sorted to the top of the file does nothing. This one
# is the only documentation of what every VITE_ knob does, and it is meant to
# be committed.
!.env.example
dist-test
