{
  "name": "seat-airlines",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A flight simulator flown by one number.",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "tsc --noEmit && vite build",
    "lint": "tsc --noEmit",
    "build:test": "esbuild src/lib/banners.ts src/lib/sectionAccess.ts src/lib/networkingApi.ts src/lib/holderList.ts --bundle --outdir=dist-test --format=esm --platform=node --define:import.meta.env='{\"VITE_BANNERS_API\":\"https://adverts.test\"}' && esbuild worker/src/networking.ts --bundle --outfile=dist-test/workerNetworking.js --format=esm --platform=node",
    "test": "npm run build:test && node test/banners.test.mjs && node test/sectionAccess.test.mjs && node test/networking.test.mjs && node test/holderList.test.mjs",
    "preview": "vite preview --host 0.0.0.0 --port 3000",
    "typecheck": "tsc --noEmit",
    "token:update": "node scripts/update-token.mjs"
  },
  "dependencies": {
    "@types/three": "^0.185.4",
    "react": "^19.2.1",
    "react-dom": "^19.2.4",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.2.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.0.0",
    "autoprefixer": "^10.4.24",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.2.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
